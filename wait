#include<bits/stdc++.h>
string uniqueChar(string str){
    // Write your code here
    unordered_set<char> temp;
    str.erase(remove_if(str.begin(),str.end(),[&](const char c){return !temp.insert(c).second;}),str.end());
    return str;
}
